# Cadre AI Take-Home Activity Timeline

- Project date: July 8, 2026
- Time zone: America/Los_Angeles
- Current phase: Verification and submission (phase history lives in the entries)
- Logging policy: Append-only; never rewrite history to make the process look
  cleaner than it was

This timeline records material actions, decisions, failures, handoffs, and
verification. Low-level terminal commands remain available in session history;
this document captures the project narrative an interviewer can follow.

Reading tip (the file is large): current status = the HIGHEST-numbered T-nnn
entry at the end of the table below plus `plan.md` checkboxes. Sections after
the table are historical snapshots kept verbatim under the append-only policy —
their "Not started" lists describe July 8, not today. Grep for the last few
`T-0` IDs rather than reading the whole file.

## Timeline

| ID | Date/time (PDT) | Actor/tool | Action | Rationale or outcome | Evidence/status |
|---|---|---|---|---|---|
| T-001 | 2026-07-08 11:45 | User / terminal | Opened a new terminal session and started the take-home work session. | Establishes the beginning of the recorded workflow. | Terminal login timestamp supplied by user. |
| T-002 | 2026-07-08, time not captured | User / Fable 5 | Submitted the candidate take-home PDF to Fable 5 (“Paper5” in the later voice note) for an initial review. | First step was understanding the assignment before implementation. | User-provided Claude session transcript. |
| T-003 | 2026-07-08, time not captured | Fable 5 | Read and summarized the seven-page challenge guide. | Identified the four hard deliverables, six example scenarios, five weighted rubric categories, and one-hour review structure. | User-provided Claude session transcript and source PDF. |
| T-004 | 2026-07-08, time not captured | User / Fable 5 | Reviewed the recruiting email and extracted deadline, OpenRouter budget, and submission mechanics. | Established Friday-morning submission target and the need to protect the metered API key. No key value was copied into project artifacts. | User-provided Claude session transcript. |
| T-005 | 2026-07-08, time not captured | User | Confirmed interview scheduling was already handled outside this project. | Removed scheduling as an immediate task and prioritized Friday submission. | User statement in session transcript. |
| T-006 | 2026-07-08, time not captured | User / Fable 5 | Brainstormed a Next.js/Vercel support concierge with chat, optional voice, grounded knowledge, escalation, rate limits, and explicit scope cuts. | Established the first architecture direction and challenged authentication/RAG as likely overbuilding. | User-provided Claude session transcript. |
| T-007 | 2026-07-08, time not captured | Fable 5 / Codex | Fable 5 hit its session limit while the user requested planning; work moved to Codex. | Preserved momentum without pretending the first session completed the plan. | Session-limit message in user-provided transcript. |
| T-008 | 2026-07-08, before 13:45 | Codex | Re-read the source PDF, rendered key pages, extracted text, and checked the empty workspace. | Verified rubric wording directly instead of relying only on the earlier summary. | `work/tmp/pdfs/cadre/` and session tool output. Complete. |
| T-009 | 2026-07-08, before 13:45 | Codex | Reviewed Cadre's current public site, strategy, engineering, case-study, and contact content. | Confirmed enough public material exists for a compact curated knowledge base. | Official Cadre source links in curated artifacts. Complete. |
| T-010 | 2026-07-08, before 13:45 | User / Codex | Selected the support-concierge direction for planning: text-first chat, progressive voice, no authentication, and no RAG in the MVP. | Aligns feature scope with the rubric's preference for a focused working product. | ADR-001 through ADR-004. Decided for planning; implementation pending. |
| T-011 | 2026-07-08, before 13:45 | User | Requested a complete public-site scrape, agent component map, decision tree, data-location plan, and documentation without wiring the application. | Defined the current phase as research and scaffolding rather than implementation. | User request in Codex thread. |
| T-012 | 2026-07-08 13:45-13:47 | Codex | Created a tested sitemap scraper and ran it against Cadre's public website. Fixed page-type singularization and Webflow content/footer extraction through failing-then-passing tests. | Produced reproducible research data while demonstrating verification of generated code. | 97/97 pages succeeded; 5 unit tests pass; `tools/scrape_site.py`; `data/raw/`. Complete. |
| T-013 | 2026-07-08, approximately 13:48 | Codex | Split the corpus into raw research, curated support knowledge, and excluded/non-default material. | Prevents all 71,000 words from being placed into every prompt and creates an auditable answer boundary. | `data/curated/`, `docs/research/`, ADR-001. Complete. |
| T-014 | 2026-07-08, approximately 13:49-13:53 | Codex | Created the agent component checklist, conversation decision tree, data/storage map, source policy, architecture design, five ADRs, open questions, and scenario coverage. | Mapped every planned component and documented why it exists, where data lives, and what remains unresolved. | `docs/architecture/`, `docs/decisions/`, `docs/plans/`, `docs/open-questions.md`. Complete. |
| T-015 | 2026-07-08, approximately 13:53-13:55 | Codex | Created Fable handoff documents, including implementation sequence and drafts for root `plan.md` and `CLAUDE.md`. | Allows Fable 5 to resume implementation without reconstructing decisions from chat history. | `docs/handoff/`. Complete. |
| T-016 | 2026-07-08, approximately 13:55 | Codex | Ran package verification: scraper tests, Python compilation, JSON/schema assertions, 97-page artifact counts, required-file checks, and secret scan. | Confirmed the planning package's internal consistency before reporting completion. | Verification output in Codex thread: 5 tests, 97 HTML, 97 Markdown, zero crawl failures, no credential values detected. Complete. |
| T-017 | 2026-07-08, approximately 13:56 | User / Codex | Requested a complete rubric checklist; Codex created a 212-item evidence-based checklist. | Converts every deliverable, scenario, rubric category, engineering tip, review segment, and submission action into an explicit gate. | `RUBRIC-CHECKLIST.md`: 18 planning items checked, 194 implementation/verification items intentionally pending. Complete. |
| T-018 | 2026-07-08 13:57 | User / Codex | Requested continuous documentation of the project timeline and an enforcement mechanism for future agents. | Made process traceability a first-class project requirement. | This file and `docs/process/timeline-protocol.md`. Complete. |
| T-019 | 2026-07-08 13:59 | Codex | Created the append-only timeline and logging protocol, then embedded the requirement into the CLAUDE draft, Fable handoff, README, and rubric checklist. | Ensures future primary agents own one canonical history while subagents return structured handoffs instead of editing it concurrently. | Timeline structure, referenced files, handoff instructions, rubric controls, and secret scan verified in the Codex thread. Complete. |
| T-020 | 2026-07-08 16:26 | User / Codex | Requested exact file locations and complete restart context for the Fable 5 session; Codex created a consolidated resume handoff with paths, status, constraints, decisions, and a copy-paste prompt. | Removes ambiguity when resuming from Fable's the local working directory and prevents planning decisions from being reconstructed incorrectly. | `FABLE-RESUME.md` and updated README; 14 referenced handoff files and the original PDF verified, future app path confirmed absent, and secret scan clean. Complete. |
| T-021 | 2026-07-08 16:29 | Fable 5 | Resumed the project from `FABLE-RESUME.md`; read the full planning package in the required order (timeline, protocol, rubric checklist, implementation brief, design, architecture docs, curated KB, scenario coverage, five ADRs, open questions, plan/CLAUDE drafts). Reviewed the package and presented the three P0 decisions (model benchmark candidates, rate-limit mechanism, real-vs-demo escalation) to the user with recommendations. | Handoff requires state summary and P0 resolution before any implementation; prevents silently guessing decisions the plan marked as deliberately unresolved. | This session's read log; decision request presented in chat. Complete (review); P0 decisions pending user input. |
| T-022 | 2026-07-08 16:35 | User / Fable 5 | User resolved all three P0 decisions: (1) benchmark three OpenRouter candidates (Claude Haiku-class, one cheap non-Anthropic, Claude Sonnet-class baseline) and select the cheapest that passes the scenario/boundary suite; (2) Upstash Redis rate limiting with per-IP sliding window plus a global daily cap; (3) real Supabase escalation persistence per ADR-005 minimal schema with 30-day retention and contact-link fallback. | Unblocks Phase 0 exit; each resolution will be recorded as an accepted/new ADR in the application repository before the affected component is wired. | User selections in Fable session (AskUserQuestion). Decisions made; ADR updates pending in app repo. |
| T-023 | 2026-07-08 16:36 | Fable 5 | Created the fresh application repository at `cadre-ai-chatbot`, seeded it with the curated KB, scenario coverage, architecture docs, and timeline; accepted ADR-005 (real Supabase escalations, 30-day retention) and added ADR-006 (Upstash rate limiting) and ADR-007 (model-by-benchmark); wrote project-specific root `plan.md` and `CLAUDE.md`; added `.gitignore` and names-only `.env.example`. This repository's copy of the timeline is now canonical. | Executes Phase 0 of plan.md with the user's resolved P0 decisions before any application code. | Root commit `e1d642a` (21 files); no secret values present. Complete. |
| T-024 | 2026-07-08 16:45 | Fable 5 | Scaffolded the Next.js 16 app (TypeScript, Tailwind, ESLint, Vitest) and built the accessible streaming chat shell: NDJSON stream protocol (text/action/done/error), request validation extracted to `lib/chat/validate` with 6 unit tests, transcript live region, composer with stop/retry, scenario-derived suggested prompts, mock endpoint. Full quality gate (`npm run verify`: lint, typecheck, 6/6 tests, production build) passed; local smoke test verified streaming and a 400 on system-role injection. | Executes plan.md Phase 1: prove UI, streaming, and error paths before any model spend; structured protocol avoids client rework when the real gateway lands. | Commits `a4a0115`, `779ca5a`; local curl output in session. Complete. |
| T-025 | 2026-07-08 16:45 | Fable 5 | Deployed to Vercel production under project `cadre-ai-chatbot` (scope enhancedintelligence-projects) and smoke-tested the public URL: GET / returns 200, POST /api/chat streams NDJSON, empty-messages payload returns 400. One process note: the first two CLI attempts used non-interactive JSON-hint mode before the explicit `--prod --scope` invocation succeeded. | Executes the deploy-early rubric requirement on day 1; deployment issues surface now instead of submission morning. | https://cadre-ai-chatbot.vercel.app ; curl outputs in session. Complete. |
| T-026 | 2026-07-09 12:24 | Fable 5 / Opus 4.8 subagents | Orchestrated three Opus 4.8 subagents (builder-prompt, builder-gateway, builder-limiter) building Phase 2 modules in parallel against interface contracts pinned before any subagent started; each returned a timeline handoff; the orchestrator independently re-ran tests/lint/typecheck, reviewed each diff, and made every commit itself (no subagent committed). | Executes plan.md Phase 2 (gateway, prompt assembler, limiter) with parallelism plus independent verification, so modules compose without rework and no generated code lands unreviewed. | Grounded prompt assembler `src/lib/prompt/{assemble.ts,knowledge.ts}` (12 tests, `d795c62`); streaming OpenRouter gateway `src/lib/gateway/openrouter.ts` (15 tests, `efa62b8`); Upstash per-IP + global daily limiter `src/lib/limits/ratelimit.ts` (11 tests, `3fa7cdf`). Complete. |
| T-027 | 2026-07-09 12:26 | Fable 5 | Wrote the chat route itself (not delegated): validate -> rate-limit -> assemble -> stream model, keyless mock fallback behind the same NDJSON protocol; ran a local smoke test. | The orchestrator owns the integration seam and the request/response contract the parallel modules plug into. | Commit `3f29276`; local smoke: streaming 200s, per-IP limiter blocked at exactly 10/min with a friendly 429 + `Retry-After`, system-role injection 400, in-memory fallback warn-once logged. Complete. |
| T-028 | 2026-07-09 12:37 | Fable 5 / Codex (GPT-5.5, read-only) | Codex ran an adversarial review of the full Phase 2 diff (5 findings: 4 accepted — 1 critical fail-open limiter, 2 reduced-severity, 1 never-throws contract violation; 1 platform-mitigated — `x-forwarded-for` spoofing, declined as code and documented since Vercel overwrites the header). builder-limiter ran one fix round; the orchestrator applied the validation/client hardening and amended ADR-006. | Adversarial review before real spend is wired catches the fail-open budget hole and injection channels while they are cheap to fix. | Commit `ccd1fa5`: limiter fails CLOSED on Redis errors incl. construction; `LIMITS.maxTotalChars=8000` whole-conversation cap (worst case was ~15k tokens/request, could exceed the $5 budget at 400/day; now ~$2/day); strict user/assistant alternation starting+ending with a user turn; client `toPayloadMessages()` also fixes a pre-existing Stop-before-first-delta bug; ADR-006 amended. `npm run verify` green — 5 test files, 55/55 tests, lint, typecheck, build. Complete. |
| T-029 | 2026-07-09 12:41 | Fable 5 | Deployed Phase 2 to Vercel production and ran a public smoke test of the hardened boundaries. | Deploy-early discipline for Phase 2; surfaces prod issues now rather than submission morning. | Deploy `dpl_2jLQWBFueRcL6Co8Mk5svjLEKmiq`, aliased https://cadre-ai-chatbot.vercel.app. Public smoke: GET / 200; POST /api/chat streams NDJSON (mock path — `OPENROUTER_API_KEY` not yet set in Vercel); forged assistant-first payload 400 (alternation); over-cap payload 400. Same non-interactive CLI JSON-hint quirk as T-025 (first call printed hint JSON; explicit rerun succeeded). Complete (deploy); live-model path blocked on `OPENROUTER_API_KEY`. |
| T-030 | 2026-07-09 13:48 | Fable 5 / Explore subagent | Evaluated porting the Seneca task-routed model registry (13-task TASK_CONFIGS pattern, explored by a read-only subagent) and declined — one model-spending task needs no router. Adopted one scaled-down idea: optional OpenRouter-native fallback routing (`OPENROUTER_FALLBACK_MODEL` adds a provider-side models array; policy: fallback = benchmark runner-up so an unbenchmarked model never answers; per-call overrides never get a fallback). | Keeps model switching a one-line env change per ADR-007 without importing a multi-task abstraction the take-home cannot justify. | Commit `507544e`; gateway tests 16/16; env var documented in .env.example. Complete. |
| T-031 | 2026-07-09 13:53 | Fable 5 / Opus 4.8 subagent | builder-benchmark built the ADR-007 benchmark harness (`npm run benchmark`): 8 scenario prompts + 2 injection probes through the production gateway and prompt assembler, live OpenRouter catalog ID verification + live pricing, automated boundary checks beside each prompt, full report to docs/benchmarks/; refuses to run without a key in .env.local. Orchestrator verified the keyless guard, lint, typecheck, and full gate before committing. | Prepares the last open Phase 2 item so the benchmark can run the moment a local key exists, spending the developer's own key rather than the recruiter's metered one. | Commit `2731537`; keyless dry-run exits 1 with zero network calls; `npm run verify` green (56/56). Complete (build); live run blocked on the local OPENROUTER_API_KEY. |
| T-032 | 2026-07-09 14:20 | Fable 5 / Opus 4.8 subagents | Orchestrated three Opus 4.8 subagents (builder-actions, builder-escalation-ui, builder-escalations) building Phase 3 modules in parallel to pinned contracts; each returned a timeline handoff; the orchestrator independently re-verified everything, wrote the route wiring itself (chat route attaches deterministic action cards after the streamed response on both real and mock paths), and made all commits. | Executes plan.md Phase 3 (informational actions + consented escalation) with parallelism plus independent verification; deterministic server-side card selection satisfies ADR-004 without a model-initiated tool call, giving better consent UX and no PII transcription errors. | Action-card selector `src/lib/actions/select.ts` (21 tests, `e09c3ce`); route attaches cards (`24c00f0`; local smoke: booking -> strategist, portal -> portal help, neutral -> none); `EscalationCard` consent form (15 tests, `31e4ab5`); the one DB write + `supabase/migrations/0001_escalations.sql`, `checkEscalationLimit` 3/IP/day (`78c30aa`). Complete. |
| T-033 | 2026-07-09 14:38 | Fable 5 / Codex (GPT-5.5, read-only) | Codex ran an adversarial review of the full Phase 3 diff (7 findings: 6 accepted, 1 declined — broad intent terms like "call center automation" surfacing a strategist card is desirable consultancy UX, documented rather than narrowed). The orchestrator adjudicated and applied every fix itself, including reversing Codex's suggested pre-parse limiter placement — the 3/day escalation cap is checked AFTER validation so a typo doesn't consume the daily quota. | Adversarial review before the mutating route ships catches a budget-coupling hole and hardens the one data write while it is cheap to fix. | Commit `8121a6a`: escalations decoupled from the chat limiter (High — its global counter guards model spend); `https` `SUPABASE_URL` + `redirect:"error"`; UI in-flight ref + `sessionStorage` session cap; migration `CHECK` constraints mirror validation; JSON content-type + 16KB content-length gate before parsing. Codex-clean: no key in client bundle, PII absent from headers/errors, `consented_at` server-stamped, XSS clean, streaming order clean, RLS closed. `npm run verify` green — 10 test files, 112/112 tests, lint, typecheck, build. Complete. |
| T-034 | 2026-07-09 14:44 | Fable 5 | Deployed Phase 3 to Vercel production and ran a public smoke test. | Deploy-early discipline for Phase 3; the mutating route and action cards are exercised on the public URL now, not on submission morning. | Aliased https://cadre-ai-chatbot.vercel.app. Public smoke: booking prompt -> action card in the stream; `POST /api/escalations` valid -> 200 `{ok, referenceId "local-1"}` (`MemoryStore` on prod — Supabase env not yet set); no-consent -> 400; GET / 200. Complete (deploy); durable persistence blocked on Supabase env vars. |
| T-035 | 2026-07-09 15:05 | Fable 5 / Opus 4.8 subagent | builder-voice implemented progressive browser voice per ADR-003: feature-detected SpeechRecognition mic (interim preview, final transcript appended for user review — never auto-sends) and opt-in speechSynthesis output (off by default, never autoplays, speaks once on stream completion, cancelled by new input/Stop); SSR-safe detection via useSyncExternalStore; controls vanish on unsupported browsers. Orchestrator verified the full gate, confirmed SSR HTML carries no voice controls, committed, and redeployed production. | Executes plan.md Phase 4: voice as pure enhancement with text canonical, matching the ADR-003 trigger analysis. | Commit `680f7ad`; `npm run verify` green (12 files, 129/129 incl. 15 voice-logic tests); SSR curl check 0 voice controls / composer present; redeployed to https://cadre-ai-chatbot.vercel.app. Complete (code); real-mic/speaker Chrome test remains a documented manual step. |
| T-036 | 2026-07-09 15:30 | Fable 5 | User provided a personal OpenRouter key for local dev (stored only in gitignored .env.local; rotation recommended since it appeared in chat). First live-model turn immediately exposed a selector bug — a fully-answered question grew an escalation form because healthy answers close with contact-offer phrasing — fixed with a live-transcript regression test (`a69cc4e`, 22/22). Ran the ADR-007 benchmark (30 calls, 3 candidates, production code path): all three passed 10/10 checks; results exposed a criterion the cost-only rule omitted (gpt-5-mini: $0.0049/suite but 5.7s median first-token). Amended ADR-007 openly (responsiveness gate ≤3s added post-results, documented as such); selected anthropic/claude-haiku-4.5 (~$0.002/turn), fallback anthropic/claude-sonnet-4.5. Pinned models in .env.local and Vercel production env. | Completes the last Phase 2 item with a measured, control-grouped selection; the amended rule is recorded honestly rather than silently rerolled. | Commits `a69cc4e`, `7a43d53`; benchmark log + docs/benchmarks/2026-07-09-model-benchmark.md (full responses); Vercel env OPENROUTER_MODEL + OPENROUTER_FALLBACK_MODEL set (key itself still pending — prod remains on mock). Complete. |
| T-037 | 2026-07-09 16:45 | Fable 5 | Provisioned durable escalation persistence per the user's decision (new isolated Supabase project): created project `otcp… (ref truncated)` via CLI, applied `0001_escalations.sql` through the Management API (direct pg and both pooler hosts unreachable on this network — IPv6-only project; token recovered from the CLI's go-keyring base64 wrapper in the macOS keychain), wired `SUPABASE_URL`/`SUPABASE_SECRET_KEY` into gitignored `.env.local` and Vercel production env, redeployed. One process note: the first env wiring silently no-opped (string replace without assertion against a scaffold that lacked the lines) and was caught because the E2E test verified the actual cloud row rather than trusting the 200 response. | Completes the last infrastructure dependency: the one database write is now durable in production, isolated from all client/business projects. | Local E2E: reference `c0cb36b5-…` + row visible via Management API with server consent timestamp. RLS proof: anon read returns `[]`, anon insert 401. Prod smoke after redeploy: reference `a3409268-…`, row `Prod Smoke` in cloud table. Vercel env now has 4 vars; only `OPENROUTER_API_KEY` (recruiter's) pending. Complete. |
| T-038 | 2026-07-09 16:55 | Fable 5 | Wrote the root README (live URL, 30-second architecture, degrade-gracefully env table, layout map, scope cuts with revisit triggers, known limitations, AI-workflow pointer), created the public submission repository under the user's personal account per their decision, pinned the repo-local git credential to that account (multi-account stomping lesson), and pushed all 28 commits. | Executes Phase 5 submission items early so Friday morning is only: recruiter key into Vercel, final live regression, Gem submission. | Commit `28d9297`; https://github.com/emilianogallardov/cadre-ai-chatbot — PUBLIC, main, 28 commits; post-push history secret scan: 0 hits for key patterns. Complete. |
| T-039 | 2026-07-09 17:00 | Fable 5 | Re-verified the bot's published contact details against the live cadreai.com/contact page (open flag since the planning package): email hello@gocadre.ai and phone (619) 324-3223 both confirmed verbatim; no public scheduling link exists, confirming the no-calendar-integration cut. | The bot recites these details in answers and cards; shipping unverified contact data would be the exact failure the knowledge policy exists to prevent. | Live fetch of https://www.cadreai.com/contact (2026-07-09) matches data/curated/knowledge-base.json verified_contacts exactly. Complete. |
| T-040 | 2026-07-10 01:30 | Fable 5 | Put the developer's personal OpenRouter key into Vercel production (piped from gitignored .env.local; to be swapped for the recruiter's metered key at submission and rotated after), redeployed, and ran the full Phase 5 live regression against the deployed URL with the real model: all six scenarios plus three boundary probes (off-topic, injection-pricing, injection-system-leak) behaved correctly — grounded answers, no invented pricing/booking/portal steps, correct deterministic action cards on every turn (strategist, portal, maturity, escalation-only-when-unanswerable, none on the healthy industry answer), refusals on both injections. Escalation write returned a durable Supabase reference. Re-ran the quality gate, both secret scans, and anonymous repo access; ticked plan.md Phase 5. | Executes the Friday verification checklist the moment the live-model path was unblocked, so submission morning is reduced to key swap + Gem form. | Deploy aliased https://cadre-ai-chatbot.vercel.app ; 9-case regression transcript in session (scratchpad live-regression-results.md); escalation reference `0cab55ac-…` (real UUID, not MemoryStore); `npm run verify` green 12 files 130/130; history + bundle secret scans 0 hits; anonymous GitHub fetch 200. Complete; remaining: recruiter-key swap, user's manual mic/mobile smoke, Gem submission. |
| T-041 | 2026-07-10 02:15 | Fable 5 / Codex (GPT-5.5, read-only) | Timeline update from the product owner: hard deadline Monday 2026-07-13, target submission Saturday, live presentation Tuesday morning. Owner decided to flip ADR-002 and add conversation storage (rationale: escalation-lead context + content-gap analytics) with a store-by-default-plus-notice posture and a user-facing private mode. Design section 1 (data model + flow) validated with the owner, then submitted to a Codex adversarial design review BEFORE any build, with explicit instruction to verify privacy/data requirements. 13 findings, all 13 accepted (2 reduced): public /privacy page (CalOPPA applies already via the escalation form; CCPA notice-at-collection), honest private-mode copy, real pg_cron retention replacing the comment-only claim (escalations retrofitted too), HMAC-signed server-issued conversation ids, idempotent upsert to kill the escalation-link race, next/server after() for post-response writes, delete-this-chat control, private+escalation semantics pinned (lead stored, conversation_id null), sensitive-info hint, schema hardening, turnId dedup, live RLS proof, honest ADR framing. Also audited the owner's first real prod conversation transcript (11 turns): grounding held throughout, zero invented Cadre facts, injection and social-engineering attempts refused; three findings feed the build — escalation card over-fires on clearly off-topic turns (algebra/pizza) and re-renders after a submission; the bot's self-description ("I don't retain memory between conversations") becomes a privacy misstatement the moment ADR-008 ships, so the KB must be updated in the same change; the bot implied a confirmation email exists (it does not — reference is on-screen only), a KB gap to close. | A pre-build adversarial review turns the owner's storage instinct into a compliant design instead of a liability, and the live transcript is the first real-user evidence shaping the UI phase. | docs/reviews/2026-07-10-codex-adr008-design-review.md (13 findings + adjudications); owner transcript pasted in session; ADR-008 document itself lands with the build. Complete (design section 1); UI section + ADR-008 + implementation next. |
| T-042 | 2026-07-10 03:20 | Fable 5 / Opus 4.8 subagents | Implemented ADR-008 server-side end to end. ADR-008 written and accepted (`80d6335`); `CONVERSATION_SIGNING_SECRET` generated and wired into gitignored .env.local + Vercel prod (presence verified). Two Opus builders ran in parallel to pinned contracts: builder-conv-store delivered signed-token module (HMAC-SHA256, timing-safe verify, never-throws), PostgREST conversation store (merge-duplicates upsert, ignore-duplicates turn dedup, 4000-char caps, status-only logging), and migration 0002 (RLS-on/zero-policies, CHECK constraints, cascade, indexes, pg_cron 30-day retention jobs for conversations AND escalations — replacing the comment-only claim); orchestrator independently re-verified (25/25 module tests, line-by-line review, one approved deviation: return=minimal on DELETE). Orchestrator wrote the integration seams itself: chat route resolves a storage plan pre-stream (private mode/missing turnId opt out; invalid token silently = new conversation), emits the signed token in a `conversation` NDJSON event, persists the completed turn via next/server after(); DELETE /api/conversations authorized by signature alone; escalations verify + upsert the conversation BEFORE the lead insert (race fix, degrades to unlinked lead); /privacy page (only enforced claims); KB `assistant-data-practices` entry fixing both live-transcript truthfulness findings (one KB regression caught by the source-URL prompt test and fixed). Migration applied to cloud project otcp… (ref truncated) via Management API (curl — python urllib now Cloudflare-blocked, error 1010) and verified: both cron jobs scheduled, RLS true on all three tables, conversation_id column present. Live RLS proof: anon SELECT conversations/messages -> 200 `[]`, anon INSERT -> 401. Reusable `chatbot-privacy-compliance` skill created at ~/.claude/skills/ by a third subagent (verified: 163-line SKILL.md + 101-line anti-patterns.md seeded with this review's findings). README rewritten for the new storage story. | Executes the ADR-008 build with the same parallel-build/independent-verify/orchestrator-commits discipline as every phase; privacy claims ship enforced, not promised. | Commits: storage foundation + seams slices; full suite 178/178, lint, typecheck green; cloud verification outputs in session. Complete (server side); client UI handoff from builder-conv-client pending, then full verify + Codex review + deploy. |
| T-043 | 2026-07-10 02:15-02:32 | Fable 5 (UI-polish workstream) | Parallel UI-polish workstream (branch `feat/ui-polish`, built in an isolated git worktree to avoid touching the conv-client workstream's uncommitted files, rebased onto `10ab4fb`, fast-forward merged to main on the owner's instruction). Four scoped changes: (1) header reduced to one line "Cadre AI Resource Agent" with a right-aligned control row the ADR-008 Private/Delete controls slot into (merged during rebase); (2) transcript scrolling reworked — auto-stick only within 40px of the bottom via a unit-tested pure helper (`stickToBottom.ts`), no yank while scrolled up during streaming, accessible "↓ Latest" pill restores bottom + sticking, prefers-reduced-motion respected; (3) live-transcript escalation-noise fix — once a follow-up is submitted (existing sessionStorage counter reused via new `hasSubmittedEscalation()`), new escalation cards are dropped at the stream-event layer while rendered ones stay; (4) consistent focus-visible states and cursor affordances across all interactive controls. Verified in a real browser: stick pinned to -0.5px during streaming; scrollTop held at 0 while content grew 1344->2133px; keyboard-only Tab->pill (:focus-visible ring confirmed) -> Enter lands at exact bottom; escalation suppression proven in both directions live (flag set -> no new card, existing card stays; flag cleared -> same ask produces a card); 375px layout checked via CSS width emulation (Chrome min window 511px on this display) — no horizontal scroll, header stays one 69px line with all controls. | The owner's first real transcript (T-041) surfaced the escalation re-render noise, and streaming yank was the worst remaining UX defect; polish lands before the Codex full-diff review so the review sees final UI code. | Commits `f9e2235`, `985c670`, `31627d4`, `2337468` on main (merged `--ff-only`, not pushed). `npm run verify` green post-rebase: 16 files, 189/189 (was 130; +11 from this workstream), lint, typecheck, production build. Known rider: two EscalationCard focus classnames landed in `31627d4` instead of `2337468`. Flagged, not changed: document `<title>` metadata still reads "Cadre AI Support". Complete. |
| T-044 | 2026-07-10 03:55 | Fable 5 / Codex (GPT-5.5, read-only) | Codex ran the round-2 implementation review of the full ADR-008 diff (verdict: fix-then-ship, 8 findings) and all 8 were accepted and fixed the same hour: conversation event now precedes action cards on both stream paths + Delete disabled while streaming (delete/store race, human-reachable path closed; ms-level residual documented as accepted with the tombstone-table production step); in-memory mirror behind sessionStorage so a blocked-storage browser can never silently lose a Private toggle (fail-safe, with tests using a throwing storage proxy); private-mode copy corrected to "new messages aren't being saved" across notice/privacy-page/KB; linkConversation switched to ignore-duplicates so linking a lead days later cannot bump last_message_at and extend retention; turnId now stable across retries of the same text so the dedup index actually covers the retry flow; 30-day copy aligned to daily-job reality; cron scheduling made idempotent via unschedule-first DO block AND proven by re-applying the migration to the live project (job count stayed exactly 2); KB self-description aligned to the real 12-turn prompt window. Also aligned the document title metadata to "Cadre AI Resource Agent" (the UI workstream's flagged rider). | The user directed a Codex review before shipping; reviewing implementation separately from design caught a second layer of defects (races, copy-vs-code drift, retry semantics) the design review could not see. | docs/reviews/2026-07-10-codex-adr008-design-review.md round-2 section (8 findings + resolutions); `npm run verify` green — 16 files, 191/191 (+2 fallback tests), lint, typecheck, build; live idempotency re-apply HTTP 201 with cron.job count 2. Complete; deploy + prod smoke next. |
| T-045 | 2026-07-10 02:45-03:05 | User / Fable 5 (UI-polish workstream) | Owner reviewed the merged UI locally and rejected two things: the transcript scrollbar rendered mid-screen against the message column ("completely unacceptable"), and the overall look needed elevation. Restructured the chat shell to the standard full-bleed pattern — the scroll container spans the viewport (scrollbar at the window edge, thin translucent styling) while text stays in a centered max-w-3xl column; header/composer borders now run full-width. Found and fixed two silent quality bugs while elevating: assistant replies rendered raw markdown asterisks (now rendered via react-markdown, the one new dependency), and the body font was overriding the scaffold-loaded Geist with Arial. Added message entrance animation and a pulsing typing-dots indicator (both gated behind prefers-reduced-motion), refined bubbles/composer/chips. Owner floated React Bits; decided against importing an animation library one day before submission — CSS-only motion, zero new client-side code paths to review. | The owner's live review is the strongest UI feedback available; the full-bleed frame is what every polished chat product does and directly kills the scrollbar-against-text defect. | Commit `ef8fc09` on main (react-markdown ^10.1.0 added). `npm run verify` green: 16 files, 192/192, lint, typecheck, build. Browser check: scroll container right edge = window width (1512/1512), bold/lists render properly, Geist active. Stick-to-bottom logic untouched (same container, tests 7/7). Complete. |
| T-046 | 2026-07-10 04:45 | Fable 5 / Opus 4.8 verifier subagent | Full requirements verification pass ordered by the owner. (1) Post-ADR-008 live regression on prod with the real model: the six brief scenarios using the planning package's exact test prompts, boundary probes (off-topic, injection), the privacy self-description case (bot recited the enforced policy verbatim to /privacy), a private-mode turn (no conversation event, confirmed), and the four named common inquiries — all green; one selector gap found and fixed with a regression test ("charge" now a strategist-card term, `ba0b945`), redeployed. (2) Wrote docs/verification/2026-07-10-requirements-verification.md: deliverables, scenarios, privacy requirements, spend protection, quality gates, each row with evidence, OPEN list for submission day. (3) A read-only Opus verifier audited all 212 items of the planning rubric checklist against repo + live site and independently re-ran the verify gate; its top gaps were adjudicated and fixed the same hour: CLAUDE.md drift corrected (ADR-008 listed; phantom zod/create_escalation claims replaced with the real validation/mutation story; transcript boundary rewritten to ADR-008 terms), plan.md updated (Saturday target, ADR-008 in scope, superseded cut annotated, Phase 6 section), ADR-002 given a supersession header. Its "dirty tree" gap self-resolved — the owner's parallel UI session landed markdown replies + full-bleed frame (`ef8fc09`, T-045) which my earlier deploy already included; suite now 192/192. Remaining OPEN items are all user-gated: Gem submission, recruiter key swap + personal-key rotation, manual mic/mobile pass, final secret re-scan; Tuesday demo-script prep flagged as the next work package. | The owner asked "are we meeting all requirements — make a checklist and go back in and check"; verification ran against reality (live site, cloud DB, git history), not against our own claims. | docs/verification/2026-07-10-requirements-verification.md; verifier report in session; regression transcripts in session records; commits `ba0b945` + this docs pass. Complete. |
| T-047 | 2026-07-10 03:55-04:20 | User / Fable 5 / Codex (GPT-5.6, read-only) | Owner asked for "more detail, more premium" and directed a Codex collaboration. Codex reviewed the eight UI files and returned a ranked top-8 (verdict: the flatness came from every region living on one plane — fix surface hierarchy, typography, and framing, not more animation) plus a DO-NOT list (no React Bits/Framer/icon libs, no particles/glow/noise, no new accent color, don't touch scroll/aria/storage logic). All 8 implemented, CSS/Tailwind only: tonal background field; composer command dock in a translucent blurred footer; editorial first-run hero + prompt chips as recommendation cards (mobile rail / sm+ grid); header brand lockup with normalized control chrome; assistant identity rows with editorial type; action cards as recommendation modules; escalation concierge form; shared .ui-lift interaction language (motion-safe). Two rider fixes: wired the parallel workstream's verifiedLinks markdown sanitizer into the Transcript render (it was defined but unconnected), and converted their composer draft-restore effect to a render-time state adjustment to clear the react-hooks lint error blocking the shared gate. | The owner's second visual review still read the page as flat/generic; an adversarial design review before styling prevented deadline-risky ornamentation (the DO-NOT list) while giving concrete, file-specific fixes. | Commit `741ac06`. `npm run verify` green: 17 files, 201/201, lint, typecheck, build. Browser check: hero/lockup/dock/modules render in dark mode, assistant identity rows + Recommended-next-step module live on a real streamed reply, 375px-emulated layout clean with no horizontal scroll, scroll/stick logic untouched. Codex review saved at scratchpad (session log rollout-2026-07-10T16-04-57). Complete; not deployed — prod redeploy owed after the orchestrator's next review pass. |
| T-048 | 2026-07-10 16:05-16:30 | Fable 5 / Codex (GPT-5.6 Sol, read-only) | Owner directed a round-3 review with the newly-GA GPT-5.6 Sol: a full-repo pre-submission sweep (prior rounds were scoped to ADR-008). Sol returned 14 findings (verdict fix-then-ship, 6.5/10) after ~50 commands and its own Gitleaks scan of all 49 commits (no secrets). Every claim was re-verified against the code before adjudication: 9 fixed the same hour, 2 already on the user-gated submission list, 3 deferred with documented reasoning. The one genuinely new HIGH both prior rounds missed: the SSE parser silently swallowed OpenRouter's mid-stream error payloads, so a provider failure after streaming began looked like a successful short answer and would receive action cards and storage — parseSseLine now throws GatewayError on top-level `error`/finish_reason:"error" (5 regression tests). Also fixed: typed rate-limit/provider messages now reach the visitor verbatim and failed sends restore their text into the composer (the old copy claimed this falsely); EOF-without-done is an error; model Markdown anchors are whitelisted to verified Cadre origins (prompt-injected phishing surface closed, unit-tested against javascript:/lookalike hosts); both mutating routes read bodies through a shared bounded byte reader (Content-Length header trust removed, 413 past cap); production with missing credentials now fails loudly (typed 503 for keyless chat, direct-contact fallback for unconfigured escalation store) instead of silently mocking; Delete-chat disabled during streaming at the button; ADR-006 spend math corrected to the Sonnet-fallback worst case with the key's credit limit named as hard backstop; historical-artifact banners on the three planning docs still describing session-only transcripts; ADR index + README counts refreshed. Coordinated live with the parallel UI session editing the same files — it folded my Chat/Composer logic edits and verifiedLinks sanitizer into its 741ac06 commit and refined the draft-restore into a render-time state adjustment. | A frontier-model upgrade the day before submission is a cheap chance at findings two earlier reviews missed — and it delivered exactly one real HIGH plus a phishing surface, validating the review-per-milestone protocol. | docs/reviews/2026-07-10-codex-round3-full-repo-review.md (full adjudication table); `npm run verify` green — 18 files, 205/205, lint, typecheck, build; Sol session log in scratchpad (codex-sol-review.log). Complete; prod deploy + smoke next. |
| T-049 | 2026-07-10 17:15-17:50 | Fable 5 / 2 Opus builders / Codex (GPT-5.6 Sol) | Owner reacted to the round-3 rating: "spec out each cleanly, have Opus subagents integrate, /codex review until all implemented." Two of round-3's deferrals were un-deferred and shipped through the full loop. (1) Pinned specs written into the repo (docs/specs/): route-level tests for the three public routes (19 cases C1-C7/V1-V5/E1-E7 with mandated mock seams and a "test code only" rule) and the ADR-007 latency gate as a pure selectModel function (T1-T6 incl. the exact gpt-5-mini history case). (2) Two parallel Opus builders implemented the specs on disjoint file sets; orchestrator independently re-verified both (all 25 case IDs present; benchmark never run — metered key). (3) Codex reviewed the increment adversarially: FIX-THEN-ACCEPT, 6 must-fix + 2 doc cleanups — the sharpest being that C1/C5/C6 asserted event membership rather than sequence (a reordered stream would have passed) and that V3 never actually reached HMAC verification (a forged-signature-accepting route would have passed). All 8 fixed: whole-sequence assertions, a real minted token with one flipped signature hex char asserted byte-identical to malformed input (no oracle), static handler imports, importActual partial mocks everywhere, fake timers for the mock stream, three stale source comments and the ADR-007 status line corrected. (4) Codex confirmation pass: 8/8 CLOSED, one comment-renumbering nit found and fixed — loop closed. Tests + comments + docs only; runtime behavior unchanged, so prod stays at the round-3 deploy. | The owner wanted the remaining reviewer-visible weaknesses closed, not explained; spec-first delegation kept two builders honest and the review loop caught assertion weaknesses that would have made the new suite security theater. | docs/specs/ (both specs); docs/reviews/2026-07-10-codex-round4-increment-review.md; `npm run verify` green — 22 files, 230/230, lint, typecheck, build; Codex logs in scratchpad (codex-round4-increment-review.log, codex-round4-confirmation.log). Complete. |
| T-050 | 2026-07-10 18:00-19:05 | Fable 5 / Opus builder / Codex (GPT-5.6 Sol) | Round-5 loop run to closure per the owner's standing until-all-implemented directive. Sol's full-repo re-review scored 7.4/10 (up from 6.5; prior fixes confirmed non-reproducing) with 8 findings; all fixed: gateway now throws on EOF-before-[DONE] and bodyless 2xx (the round-4 fix had covered only the client) plus a 45s provider timeout that preserves client aborts; synchronous in-flight guard kills the double-send race; Delete route gets a byte-bounded body read and a new per-IP daily cap (generalized fail-closed daily limiter, rl:del, tested unmocked); benchmark scenario checks replaced with discriminating assertions built by an Opus builder to a pinned spec. The confirmation pass (7.7/10) then verified 4 closed and caught real problems in our own fixes — most importantly that the strengthened benchmark regexes only matched ASCII apostrophes, falsifying gpt-5-mini's 3/10 (five of seven failures were false negatives) — so the checks were corrected (Unicode normalization, then a broadened refusal cluster for a legitimate role-persistence deflection) and the harness re-run to an honest final: all three models 10/10 on substance, gpt-5-mini excluded by the 3s latency gate (5.3s median; intermittent empty completions in earlier runs — reasoning-token burn), claude-haiku-4.5 winning at 977ms. Escalation copy de-promised everywhere it lived (success panel, deterministic card in select.ts, KB self-description, README limitation); README/plan corrected (benchmark story, production-degrade scope, delete-cap env var); timeline structurally repaired (unified table, updated header/pointer, personal path and project ref redacted, original preserved in session records). Prod redeployed and smoked: new card copy live, event protocol intact. | The reviewer reviewing our fix to its own finding caught our fix being wrong — that loop, not any single score, is the quality mechanism; the repo now documents the correction trail instead of the flattering first number. | docs/reviews/2026-07-10-codex-round5-full-repo-rereview.md; docs/benchmarks/2026-07-11-model-benchmark.md (final run); ADR-007 addenda; `npm run verify` green — 23 files, 264/264; deploy aliased + smoke in session records. Complete. Remaining user-gated: Upstash provisioning (limiter durability — the one round-5 item that needs the dashboard), recruiter key swap + rotation, manual mic/mobile pass, Gem submission. |
| T-051 | 2026-07-10 19:10-21:05 | User / Fable 5 / 2 Opus builders / Codex (GPT-5.6 Sol) | Owner asked the scale questions no review round had: how many users, how long a session before context issues, what happens under load. The session question exposed a REAL bug five rounds missed — the client sent the entire transcript every turn while the server rejects >8000 total chars, so normal sessions died mid-conversation around turn 10-12 with a validation dead end. Shipped through the full spec->builders->review loop: (1) client rolling window valid by construction (single window constant shared via LIMITS; overflowing same-role merges keep the NEWEST content so an edited retry is what the model actually receives; guarantee precisely scoped to send()'s user-ending precondition) with P1-P6 payload tests and 50-exchange endurance tests through the real route (S1/S2, incl. 1500-char turns); (2) spend-guarded dependency-free load harness (localhost-only by default, mock-marker preflight that refuses fan-out against a keyed server — proven functionally both ways, redirect:error, 60s per-request deadline, verdict fails on everything except 429); (3) docs/SCALING.md with derived-and-cross-checked cost math (~\$0.0024 avg/turn, ~\$0.0066-\$0.0197 bounded worst) and measured results: 6,650 zero-error mock-pipeline requests across 25/50/100 concurrency (TTFB p95 <=12ms, throughput linear to 139/s on one process), a default-caps burst where the limiter served exactly its budget and 429'd the rest, and a controlled live-prod burst (12 real turns, TTFB p50 1218ms, 3 correct 429s). Codex round-6 review: FIX-THEN-ACCEPT, 8 findings incl. two HIGHs in our own new code (retry-swallowing truncation; a PASS verdict that ignored failures) — all fixed; its confirmation pass then caught the error-masking fix itself being incomplete (done-after-error overwrote the terminal state) plus a process defect (plan claimed completion before this entry existed), both closed. | The owner's product questions found what adversarial code review could not — reviewers checked requests, not sessions; the fix makes long sessions work by construction and the scaling story is now measured, not asserted. | docs/specs/2026-07-10-session-window.md + -load-test-and-scaling.md; docs/reviews/2026-07-10-codex-round6-scaling-review.md; docs/SCALING.md; `npm run verify` green — 24 files, 273/273; loadtest logs in session records. Complete pending prod deploy + smoke in the same commit cycle. |
| T-052 | 2026-07-10 21:10-22:05 | Fable 5 / Codex (GPT-5.6 Sol) | Closed T-051's pending items and ran a fresh full-repo GRADING pass with Sol: the round-6 build was deployed to prod (`eafb864` aliased) and smoked live — a windowed 8-exchange-deep session payload streamed a real answer where the pre-fix client would have died on the 8000-char validation cap; docs-only follow-up `7be467e`. Sol's grade: **8.3/10, "strong senior take-home and a credible hire signal"** (up from 6.5 -> 7.4/7.7 across rounds), per-area A-/B+ with no new runtime blocker found. Its four residual embarrassment items fixed same hour: this entry (deploy evidence was real but unrecorded — the claim now has its artifact), the three-way spend-story contradiction unified on SCALING.md's derived math (~\$2.62 Haiku / ~\$7.86 all-Sonnet worst at the 400 cap, prepaid credit as hard stop, exhaustion-not-overspend stated honestly), the Private-mode TOOLTIP still carrying the round-2 overclaim ("won't save this chat" -> "new messages aren't saved" — the third copy surface for this claim; tooltips are copy too), and ADR-006's "closing the forged assistant-history channel" overclaim rescoped to shape-only with the real containment named. Also built the reusable `deployed-chatbot-requirements` skill (10 domains mapped to OWASP LLM Top 10 2025 + WCAG/NN-g UX baselines from published checklists, verification method per item, 14 field anti-patterns) and audited this bot against it. | The owner asked for the grade and "does it handle everything a deployed chatbot should" — the honest answer needed a researched external yardstick, not our own rubric; the audit's open items are operations-tier (CI, observability, feedback affordance) and the standing user-gated list, all documented as triggers. | codex-round7-grade.log in session records; docs/SCALING.md §2a canonical math; ~/.claude/skills/deployed-chatbot-requirements/; `npm run verify` green post-fixes; deploy + smoke in session records. Complete. |
| T-053 | 2026-07-10 22:10-22:45 | Fable 5 | Closed the cheap gaps from the deployed-chatbot-requirements audit (skill at ~/.claude/skills/, built from OWASP LLM Top 10 2025 + WCAG/NN-g sources): GitHub Actions CI running the full envless gate on every push (the suite passes with zero configuration by design, so CI needs no secrets); GET /api/health uptime hook reporting subsystem configuration booleans without values (ok/degraded/misconfigured — 'degraded' = documented fallbacks; 3 route tests incl. a no-secret-leak assertion); WCAG touch-target floor (44px on coarse pointers via media query, desktop chrome unchanged); contrast bumps for inactive toggle labels (zinc-400→500 light, 500→400 dark) and the composer placeholder; README limitations gain English-only and no-feedback-affordance entries with production triggers (thumbs deferred deliberately: it is new data collection and ships with its privacy-page/KB update under this repo's storage rule — not the night before submission). | The audit's remaining gaps were operations-tier; these five were low-risk and reviewer-visible, the sixth (feedback) was scope with privacy surface and got a documented trigger instead. | .github/workflows/ci.yml; src/app/api/health/; globals.css touch-target rule; `npm run verify` green — 25 files, 276/276; CI run + prod smoke evidence follows in session records. Complete. |
| T-054 | 2026-07-10 22:50-23:20 | Fable 5 / Codex (GPT-5.6 Sol) | Codex round-8 review of the T-052/T-053 increment: FIX-THEN-ACCEPT, no HIGHs, 6 findings — all closed same hour. Dark-mode contrast: the placeholder bump had regressed dark mode (zinc-500 on zinc-900 ≈ 3.67:1) and four PRE-EXISTING eyebrow labels shared the problem → dark:text-zinc-400 across Composer/Chat/Transcript/ActionCardView/EscalationCard. Touch targets: the coarse-pointer rule missed the notice-line private-mode toggle and the conditional Latest pill → shared .tap-target utility applied (the first attempt produced a duplicate className JSX prop the gate caught — merged). CI hardening: permissions contents:read + persist-credentials:false (the 'no secrets' claim was not literally true — GitHub's job token exists; now scoped read-only and not persisted), comment corrected to 'every main push and pull request'. Health endpoint: Cache-Control no-store + H4 test, and the transparency decision (configuration booleans public) documented in-code as deliberate-for-public-source with the closed-source alternative named. Two historical docs annotated as superseded by SCALING.md §2a rather than rewritten. | Even a polish increment earns its adversarial pass — it caught a contrast regression inside an accessibility fix and an insufficiently-scoped CI token, both exactly the kind of thing a reviewer notices. | codex-round8-increment.log in session records; `npm run verify` green — 25 files, 277/277; CI + deploy evidence follows. Complete. |
| T-055 | 2026-07-11 01:40-02:35 | Fable 5 / Codex (GPT-5.6 Sol) | Owner asked for the required fixes plus a review of the UI changes merged cleanly into main. State check: the UI work was ALREADY merged (`feat/ui-polish` tip `2337468` is an ancestor of main — the stale branch pointer deleted) and the round-8 gate green, so the substance became (a) backfilling the missing docs/reviews/ records for rounds 7-8 plus a rounds-7/8 workflow-log entry (`455ade9` — every review round now has its artifact), and (b) Codex round 9: the first CONSOLIDATED UI-layer review (components, hooks, client storage, markdown rendering, privacy page — prior rounds reviewed increments or the whole repo, never the merged front-end as a unit). Verdict FIX-THEN-ACCEPT: 1 HIGH, 8 MED, 2 LOW — all closed in `3c8a146`. The HIGH was a delete/send race six prior passes missed: a send started while the DELETE fetch is pending reuses the token being deleted and the post-stream store recreates the conversation server-side after the client cleared its only deletion handle → synchronous module deletion flag now guards send(), disables the composer, and unlinks mid-delete escalations. Also closed: markdown image gating (model-emitted `![x](url)` rendered arbitrary remote images — now alt-text-only, pinned by new rendering-safety tests against the exported components), draft-restore no longer overwrites text typed during a streaming reply, speech cancel() detaches onresult before abort so a trailing final result can't repopulate the sent composer, keyboard-focusable transcript scrolling (tabIndex + visible focus), aria-busy streaming announcements, focus management on delete/prompt-pick/escalation-confirm transitions, escalation session cap now counts under read-ok/write-throw storage (mirror advances, reads take max), residual dark-mode contrast (per-message Cadre AI eyebrow + privacy-page labels — the orchestrator's pre-read flagged the eyebrow independently before the review returned), reduced-motion mic pulse, sessionStorage 'new tab' claim reworded to honest browsing-context semantics. Mid-loop, stale macOS ' 2.ts' duplicate artifacts in .next/types broke typecheck — cleared and re-verified. | A focused lens finds what general passes miss: rounds 3-8 read this code repeatedly, but only the UI-scoped prompt surfaced the delete race and the image-gating hole; and review records are part of the deliverable, so rounds 7-8 got theirs backfilled before round 9 created a third gap. | docs/reviews/2026-07-11-codex-round9-ui-layer-review.md; codex-round9-ui-review.log (172K tokens, session records); `npm run verify` green — 26 files, 287/287 (10 new tests) + production build; commits `455ade9`, `3c8a146` + docs commit; pushed to origin/main; prod deploy + smoke evidence in session records. Complete. |
| T-056 | 2026-07-11 02:50-03:45 | User / Fable 5 / 5 audit subagents (Sonnet) | Owner directed a live multi-session test through the real browser: "we need concurrent sessions." Executed via the Claude-in-Chrome extension against prod (`e0f7e0b`): (1) FIVE CONCURRENT SESSIONS — 5 tabs, 4 rounds, each round's sends fired back-to-back (<2s spread) so five model streams ran simultaneously; 20/20 success, zero console errors/alerts/stuck streams; pooled TTFB p50 972ms / p95 1,653ms with warm rounds (799–1,298ms) beating the 1,218ms single-source baseline. Boundary probes fired DURING concurrency all held: pricing refusal (no `$` anywhere in the thread), injection deflected with clean next-turn recovery, no SOC2/HIPAA guarantee, no booking-capability claim, escalation card with honest copy. (2) STORAGE AUDIT — the 5 captured conversation IDs held exactly 40 Supabase rows (4 exchanges × 2 × 5), strict alternation, user messages byte-for-byte faithful, zero duplicates under concurrent post-stream writes; five parallel audit subagents (one per session) independently verified 20/20 tasks PASS and all five converged on the same design confirmation: user+assistant rows share one created_at (the documented ADR-008 single insert), so browser network timing — not DB timestamps — is the latency source of record. (3) ENDURANCE — one session driven 40 exchanges by an in-page loop: 40/40, zero errors; median TTFB turns 1–20 = 956ms vs turns 21–40 = 934ms — LATENCY FLAT WITH DEPTH, the round-6 O(1)-window claim now proven live at 3–4× the old death depth (pre-fix clients died ~turn 10–12); the turn-35 memory probe returned the honest correct-by-design answer (bot states it only sees recent messages); 80 rows stored in perfect alternation. Measurement note: mid-run background-tab timer throttling inflated the DRIVER's wall-clock for a few turns — network-level Performance API timings (used for all reported numbers) are unaffected. Spend: 60 live turns ≈ 15% of the daily cap. | The owner wanted proof, not claims, that real concurrent users and a long real session work on the deployed product — and the multi-agent audit pattern (driver orchestrates, independent agents verify per-session) kept the verification independent of the operator. | docs/benchmarks/2026-07-11-live-browser-sessions.md (full report incl. per-turn latency series + audit addendum); docs/SCALING.md measured-results rows added; raw data live-audit-*.json in session records; conversations 6f81935f/0d3cafc1/5137617f/39527492/ae8166bf/20c2aa5c in Supabase. Complete. |
| T-057 | 2026-07-11 03:05-04:00 | User / Fable 5 / Codex (GPT-5.6 Sol) | Owner asked whether the bot answers "not just technically but in a way that is useful and builds trust," then chose fixes from the brainstorm (voice = "I" + Cadre third person; scope = prompt fixes + full test battery). Codex round 10 reviewed BOTH the T-056 evidence and all 120 stored live messages against the KB: FIX-THEN-ACCEPT, 7 MED + 2 LOW. Part A (docs overclaims — all corrected in place): concurrency and "O(1) proven live" claims scoped to what one uncontrolled run shows, "byte-for-byte" narrowed to user messages (assistant bodies were length-checked), chronology fixed from stored-row timestamps (runs 02:34-02:50 PT, not the ~02:50-03:45 the docs claimed — T-056's time labels stand corrected by this entry per append-only discipline), the raw 40-turn latency series preserved as a report appendix, quantiles labeled nearest-rank. Part B (answer quality): the CTA treadmill (contacts in 49/60 live replies) traced to the prompt's UNCONDITIONAL "offer the verified contact route" instruction, and one real trust defect the orchestrator's own read missed — synthesis overreach (KB facts recombined into "route optimization"/"reduce costs" capability claims for a logistics prospect). Fixed as prompt text in assemble.ts (voice pin, contact cooldown, synthesis-grounding incl. "brevity is not a license to state possibilities as facts", formula breakers, stale no-markdown rule reconciled) with every rule pinned by unit tests. New live harness scripts/quality-metrics.ts (npm run quality): 12 scripted turns with per-turn contact-expectation labels; baseline reproduced every defect (9/12 contact, 5 grounded-turn violations, synthesis overreach); enforcement aggregates TWO runs (n=12 rates flap at thresholds — observed, then designed around) with the synthesis probe required to pass EVERY run. Final: 10/24 contact, 1 grounded violation, 0 voice violations, 0 synthesis failures — PASS. The loop caught its own author twice: the first cooldown wording was logically disjunctive (rewritten to (a-or-b)-AND-not-visible), and the benchmark regression rerun surfaced the refusal-cluster false-negative class a THIRD time ("vary your phrasing" produced clean novel deflections) — cluster broadened, observed shapes pinned as tests, haiku restored to 10/10 with the same ADR-007 selection. | Trust is a product property, not a code property — the stored transcripts were the artifact under review, and the fixes are measured (baseline vs after artifacts committed), not asserted. | docs/reviews/2026-07-11-codex-round10-evidence-and-answer-quality.md; docs/specs/2026-07-11-answer-quality-voice-and-cta.md; docs/benchmarks/quality-metrics-{baseline,after}.json; docs/benchmarks/2026-07-11-model-benchmark.md (post-fix rerun, haiku 10/10); ADR-007 third addendum; `npm run verify` 26 files / 295 tests green; codex-round10-quality.log (session records); commits + prod deploy evidence follow. Complete. |
| T-058 | 2026-07-11 09:55-10:10 | User / Fable 5 | Owner asked the pre-submission failure-mode question: "pretend we try to hit Haiku and something happens — do we have a backup?" The fallback chain existed in config (`OPENROUTER_MODEL` + `OPENROUTER_FALLBACK_MODEL` both verified present in Vercel prod, ADR-007 policy) and the request-body shape was unit-tested, but the path had never been exercised against the live provider. Three failure injections sent with the gateway's exact body shape (personal key, ~$0.07): (1) invalid primary ID without fallback → HTTP 400, expected control; (2) invalid primary ID WITH fallback array → HTTP 400 — **finding: OpenRouter validates every model ID up front, so the fallback does NOT cover config typos**, which fail loud as the typed error (post-deploy smoke turn is the guard); (3) valid primary erroring at runtime (8k-context gemma-2-9b-it fed ~20k tokens) with the Sonnet fallback → stream served by `anthropic/claude-4.5-sonnet-20250929` via **Amazon Bedrock** — the outage class IS covered live, and the Bedrock routing demonstrates OpenRouter's own multi-provider layer under each model (an Anthropic-API outage alone doesn't even need the model fallback). Residual single points named honestly: the one OpenRouter key (no auto-rotation; death = typed error + contact routing, fix = env swap) and OpenRouter itself (floor = same typed error). Documented in a new benchmark record + SCALING §2c nuance. | Submission-readiness diligence: the resilience story should be evidence, not architecture diagrams — and the injection found a real coverage boundary (invalid-ID class) worth stating before an interviewer finds it. | docs/benchmarks/2026-07-11-fallback-injection-test.md; docs/SCALING.md §2c; `vercel env ls production` (names only); commit follows. Complete. |
| T-059 | 2026-07-11 10:20-10:35 | User / Fable 5 | Owner asked for an agent-discovery / best-practices review of the repo's instruction files. Research (HumanLayer, AGENTS.md spec guides, cross-tool portability writeups) established the 2026 standard: `AGENTS.md` at the root is the canonical cross-tool instruction file (Codex, Cursor, Copilot and 60k+ repos read it natively) and `CLAUDE.md` should IMPORT it via `@AGENTS.md` — Claude Code resolves `@` imports, other tools do not. This repo had the direction inverted: full content in CLAUDE.md and a bare `@CLAUDE.md` in AGENTS.md that non-Claude tools cannot resolve. Swapped: content now lives in AGENTS.md (header notes it is canonical), CLAUDE.md is the one-line `@AGENTS.md` import. Historical timeline references to "CLAUDE.md" remain accurate (the file exists and resolves to the same content); the two src comments citing "CLAUDE.md architecture rules" left untouched deliberately — a parallel session is actively editing src/ (mobile polish in globals.css/Composer.tsx observed mid-review) and this commit stays strictly disjoint from it. Content itself unchanged except the header + canonicality note, so every prior review of these instructions still applies. | Cross-tool agent discovery: any AI tool a reviewer points at this public repo now finds the full instructions under the standard filename instead of a Claude-specific import stub. | AGENTS.md; CLAUDE.md; research links in session records; commit follows. Complete. |
| T-060 | 2026-07-11 10:35-10:55 | Fable 5 / 2 blind subagents | Instruction-discovery audit: does a FRESH agent pointed at this repo find and follow the governance? Two blind read-only subagents (no project knowledge, told to log every step and work naturally): one cold "review this project" brief, one asked to plan a small UI change. BOTH discovered CLAUDE.md/AGENTS.md at step 2-3 and reordered their work because of it — reading timeline tail + plan.md + git status before source exactly per the Required Workflow. The task-planning agent independently reconstructed the complete process chain (timeline start/outcome entries with evidence, KB-mapping constraint for chip text incl. that pricing/portal/booking chips would invite prohibited claims, doc/code reconciliation in the same commit, verify gate, deploy + live smoke, push≠deployed), and resolved global-vs-project instruction tension in the project's favor. Friction found → fixed: empty `docs/handoff/` placeholder removed (name promised current-state docs it did not contain); reading tip added to this file's header (65KB overflows a naive tail; the historical "Not started" tail sections can misread as current). Noted, deliberately NOT fixed here: SuggestedPrompts.tsx "six evaluated scenarios" comment vs four chips — that file is mid-edit by a parallel session (mobile-polish pass observed in src/); flagged for that session or a follow-up. Delegations recorded in docs/ai-workflow-log.md per the Subagent-use rule. | The instruction system itself is a deliverable for an FDE take-home — this audit shows the repo onboards an unfamiliar agent correctly, and the two flaws it surfaced were navigation flaws, not rule flaws. | docs/ai-workflow-log.md (audit entry); agent step logs in session records; this file's header; commit follows. Complete. |
| T-061 | 2026-07-11 10:15-10:45 | User / Fable 5 (mobile workstream) | Owner opened the app on an iPhone simulator and rejected the mobile rendering ("looks terrible — full mobile rework"). Root cause found by CSS forensics: T-053's WCAG touch-target floor was UNLAYERED, and unlayered author CSS beats everything in Tailwind's @layer utilities — so on every coarse-pointer device its min-width:44px silently replaced the prompt rail's min-w-[15rem], crushing four suggestion cards into ~90px columns; every desktop check passed because desktop pointers are fine, and the earlier 375px verification only emulated width, not pointer coarseness. Fix: the floor moved into @layer components (explicit utility min-widths now win; 44px floor still applies everywhere else). Mobile pass in the same commit: 16px input font below sm on composer + escalation fields (kills iOS Safari's focus auto-zoom), edge-to-edge snap-scrolling prompt rail with a peeking next card, overscroll-behavior contain on the transcript (no pull-to-refresh mid-chat), -webkit-tap-highlight-color transparent, brand lockup now shown from 400px so current iPhones (402pt) keep the header identity, and T-060's flagged comment rider corrected (four prompts, not six). One self-inflicted build break mid-pass — 'min-w-*/min-h-*' inside a CSS comment terminates the comment at the '*/' — caught by the dev overlay and fixed. Verified live on an iPhone 17 Pro simulator (iOS 26.5, real WebKit coarse pointer): rail renders at full card width and snap-scrolls, prompt tap streams a full turn with markdown + action card, keyboard opens with NO page zoom, delete-chat icon appears post-conversation. | The mobile viewport is the primary form factor for a public support chatbot, and the defect class (pointer-media CSS) was invisible to every desktop-browser check made across ten review rounds — only a real touch-device runtime surfaced it. | Commits 4ed0595 + this docs commit; npm run verify green — 26 files, 295/295, lint, typecheck, production build; simulator evidence in session records (before/after screenshots). Complete. |
| T-062 | 2026-07-11 11:00-11:40 | User / Fable 5 / Codex (GPT-5.6 Sol) / 3 blind subagents (Haiku, Sonnet, Opus) | Owner directed a final buttoned-up audit: Codex round 11 (docs/reviews/2026-07-11-codex-round11-buttoned-up-audit.md) alongside a THREE-MODEL blind instruction-discovery panel. Panel results — every model found and followed the governance: Haiku (cold review) read CLAUDE.md first, resolved the @AGENTS.md import, used the T-060 reading tip, followed the Required Workflow order; minor reliability caveats (called it 9 rounds not 10, invented "git worktrees" as the parallel-session mechanism, implied Upstash provisioned). Sonnet (task plan, 'Clear conversation' shortcut) was the standout: refused to guess the feature's privacy semantics (clear-visible vs new-conversation vs the dangerous delete-server-data reading), caught that a net-new feature on submission day would violate plan.md's own no-silent-scope-change rule, self-reported exceeding its command allowlist, and independently found the rubric-checklist ghost + the CLAUDE.md comment residue. Opus (skeptical-reviewer brief) ran a secrets sweep FIRST (clean: only .env.example tracked), verified README claims against route code, and surfaced the one non-fixable flag: the process footprint itself (38 docs, 70KB timeline, AI-orchestration front-and-center) is what a skeptic reacts to first — interview-prep material, not a repo defect. Codex verdict FIX-FIRST (3 HIGH / 3 MED / 2 LOW, zero product-code findings): stale benchmark story in README/SCALING (said all three models passed substance; final run has gpt-5-mini at 5/10) corrected; requirements-verification + plan.md refreshed (295 tests, T-061, AGENTS.md canonical, mobile done); rubric-checklist mandate reworded as external in AGENTS.md + timeline-protocol; knowledge-boundary claim made defensible (was "cannot leak facts it was never given"); src comment residue + AGENTS.md path shorthands fixed; scaffold residue removed (5 unreferenced default SVGs deleted, next.config comment made intentional; favicon left default deliberately). macOS-duplicated .next artifacts cleared and the gate re-run FRESH: 26 files / 295 tests / clean build, exit 0 — with the parallel session's in-flight mobile WIP compiling green in the same tree. Remaining HIGH is the user-gated Upstash step already on the checklist. | Two independent audit methods (Codex sweep + blind Sonnet agent) converged on the same residue findings — the sweep is near-exhaustive; everything repo-side is now buttoned. | docs/reviews/2026-07-11-codex-round11-buttoned-up-audit.md; README.md; docs/SCALING.md; docs/verification/2026-07-10-requirements-verification.md; AGENTS.md; docs/process/timeline-protocol.md; plan.md; src/lib/gateway/openrouter.ts:4; src/lib/prompt/knowledge.ts:4; next.config.ts; public/ SVGs removed; fresh verify exit 0; commit follows. Complete. |
| T-063 | 2026-07-11 10:30-10:50 | User / Fable 5 (mobile workstream) | Owner reviewed the typing experience on the simulator and rejected it (composer floating mid-screen over a dead zone, header scrolled behind the status bar, privacy notice crowding the keyboard) and added two product asks: no way to start a new conversation without reloading, and the suggested-prompt cards read as big boxy squares. Shipped as one client-side pass: (1) new useVisualViewportPin hook — iOS Safari overlays the keyboard without resizing the layout viewport, so the shell's height now tracks visualViewport.height (published as --vvh, consumed as h-[var(--vvh,100dvh)]) with a guarded document-scroll re-pin, and a Transcript ResizeObserver keeps a bottom-pinned reader pinned through keyboard open/close (interactive-widget viewport meta is still Chrome/Firefox-only, so the JS pattern is the production route); (2) New-chat header control shown once a conversation exists — aborts any stream, clears transcript + conversation token so the next turn mints a fresh server-side conversation; the old one remains stored per the collection notice, and losing its Delete handle (the token) is documented at the callsite; (3) suggested prompts reworked from dock cards to a quiet 'Try asking' line-item list under the first-run hero (hairline dividers, 44px tap-target rows), slimming the mobile dock to composer + notice; empty state top-anchored (bottom-pinning had pulled the hero's monogram under the header) and the brand lockup yields to the fuller control row only on narrow screens once conversation controls appear. One lint catch made the code better: the ref-write-during-render for the empty-state guard was restructured into effect-synced state. Verified on the iPhone 17 Pro simulator end to end: keyboard opens with header pinned and composer seated on the keyboard, prompt pick streams, New chat resets to hero with focus on the composer and an aria-live announcement. | The owner's live typing session is the highest-signal review surface the project has — the keyboard failure is invisible in any desktop browser, and both product asks (restart affordance, quieter prompts) came from using the product, not reading it. | Commit 841ab5f; npm run verify green — 26 files, 295/295, lint, typecheck, production build; before/after simulator screenshots in session records. Complete pending prod deploy + smoke in this same cycle. |
| T-064 | 2026-07-11 10:50-11:00 | User / Fable 5 (mobile workstream) | Owner benchmarked the dock against ChatGPT mobile (screenshot pair): the collection notice should read as a barely-visible centered caption, not a left-aligned text block crowding the composer. Presentation-only change — the enforced ADR-008 notice copy and both links are untouched: centered text-[11px] caption, zinc-500 (holds the round-8 ≥4.5:1 contrast floor; deliberately NOT ChatGPT's lower-contrast grey), tighter composer/dock spacing; the transient status announcement matches. Side effect: the freed height lets the full 'Try asking' list fit above the fold on a 402pt iPhone. | The notice must stay honest AND legible while the owner wants it visually quiet — compact centered presentation satisfies both without touching reviewed compliance copy. | Commit (this cycle); npm run verify green — 26 files, 295/295; simulator screenshot in session records. Complete with prod deploy + smoke this cycle. |
| T-065 | 2026-07-11 17:30-18:05 | User / Fable 5 | Owner asked for out-of-the-box "bulletproof" testing beyond T-056 (concurrency/endurance). ~40 live probes from a real Chrome tab against prod, speaking the exact /api/chat wire protocol, every stored row audited back in Supabase and ALL test data deleted after (final sweep 0 rows across every marker). ZERO defects. Validation gauntlet: empty/null/missing/wrong-role/non-string-content/non-alternating all typed 400; 70KB→413; non-string conversationToken ignored (fail-open tokenless). Adversarial content: bidi/zero-width/RTL-override handled clean; CJK+emoji+combining → correct pricing refusal; system-prompt exfiltration REFUSED with zero grounding leaked; <script>/<img onerror>/markdown-image injection — model declined to echo + renderer gates images to alt-text (defense in depth). Rate limit: per-IP 10/min tripped exactly on the 11th → 429 + Retry-After:28 + honest copy, rejected turn spends no model. Storage/privacy (ADR-008) audited row-by-row: private mode = 0 rows + no token; normal = signed token + stored pair; storage requires valid-UUID turnId as dedup key (fails safe to no-store — correct-by-design, first mistaken for a bug then confirmed in planStorage source); continuity attaches to same conversation; TRUE retry (same turnId+token) dedup held at 4 rows (no double-store); concurrent parallel turns → distinct conversations, zero cross-contamination; abort mid-stream → after() persisted the COMPLETE turn (no truncated partial). Delete (ADR-008 #7): tampered/garbage token → 400 (HMAC guards unauthorized delete); valid delete → 200 cascade-purge (0 rows + conversation gone); double-delete idempotent. Only non-green = the already-tracked Upstash durable-cap item (/api/health durableRateLimit:false). | The boundaries a hiring reviewer pokes — malformed payloads, prompt injection, cross-session leakage, retry double-writes, unauthorized delete — all held; measured against the DB, not asserted. | docs/benchmarks/2026-07-11-durability-edge-case-tests.md; Supabase row audit (session records); prod probed at 999c988→0d0d302; commit follows. Complete. |
| T-066 | 2026-07-11 11:20-11:30 | User / Fable 5 (mobile workstream) | Owner: the privacy caption must disappear while typing, and once in a conversation it should not appear at all. Scoped the ADR-008 collection notice to the first-run screen only — that IS the at-or-before-collection moment the notice exists for, and every New chat returns the visitor there before their next stored message; in-chat, the header's Private toggle remains the persistent control and the copy is unchanged everywhere it still renders. Keyboard-up hiding shipped as pure CSS: @media (pointer:coarse) .chat-shell:has(#chat-input:focus) — the first JS attempt (innerHeight minus visualViewport.height threshold) was built, tested on the simulator, and REVERTED because modern iOS Safari shrinks innerHeight together with the visual viewport, so the delta never trips; composer focus on a coarse pointer is the reliable keyboard signal. Also unstuck two tooling traps this cycle: the simulator's software keyboard silently stopped appearing after idb ui_type (hardware-keyboard mode latches; fix = per-UDID ConnectHardwareKeyboard-<udid> default + device reboot), and the dev server served stale CSS for an edited globals.css (turbopack cache corruption follow-on; fix = clean .next/dev restart, verified by curling the served chunk before re-testing). Verified all three states live on the simulator: first-run shows the caption, keyboard-up hides it, in-chat it is absent with the dock reduced to the composer. | Notice-at-collection is satisfied at the point of first data entry, not by permanent screen residency — the owner's ChatGPT benchmark and the compliance requirement are both met by scoping, not weakening, the artifact. | Commit (this cycle); npm run verify green — 26 files, 295/295; three-state simulator screenshots in session records. Complete with prod deploy + smoke this cycle. |
| T-067 | 2026-07-11 11:40-11:50 | User / Fable 5 (mobile workstream) | Owner flagged a visible gap between the composer and the keyboard while typing. Diagnosis split it honestly: the lower half is Safari's own bottom chrome (address pill + form accessory bar — unreachable from a web page), but the strip directly under the composer was ours — the dock kept its pb-[max(0.5rem,env(safe-area-inset-bottom))] home-indicator padding (34pt on this device) with the keyboard up, and the keyboard covers that zone, making the inset dead space. Fixed with the same pointer-coarse :has(#chat-input:focus) signal from T-066: dock padding collapses to 0.5rem while the composer is focused, restoring the full safe-area inset the moment the keyboard closes. Also re-hit the dev server's stale-CSS failure mode from T-066 (globals.css edits not recompiled) — same clean .next/dev restart, and the curl-the-served-chunk-before-testing rule proved its worth by catching it before a false negative on the device. | The visual gap read as our layout bug; splitting owned padding from browser chrome keeps the fix honest and the safe-area behavior correct when the keyboard is down. | Commit (this cycle); npm run verify green — 26 files, 295/295; before/after simulator screenshots in session records. Complete with prod deploy + smoke this cycle. |
| T-068 | 2026-07-11 12:00-12:20 | User / Fable 5 (mobile workstream) | First REAL-DEVICE test: the owner used the deployed product on their iPhone and filed four defects. Shipped three, diagnosed the fourth: (1) read-aloud REMOVED by owner decision — speechSynthesis never produced audible output on their phone (iOS mutes it behind the silent switch and autoplay policy; native screen readers do the job better); header toggle, useSpeechOutput hook and its 9-test suite deleted, README/plan annotated (Phase-4 checkbox kept with a removal note rather than rewriting history). (2) Mic died ~5s into a natural pause — root cause: recognition ran single-utterance (continuous=false); now continuous=true plus auto-restart on the engine's silence timeout with a 2-minute session cap, fatal errors (not-allowed/audio-capture) excluded from the restart loop, and unmount clears keep-alive BEFORE abort so onend cannot revive an unmounted component. Simulator cannot prove the restart path (no real audio input) — flagged for the owner's on-device retest. (3) Long questions cut off in the one-line input — replaced with an auto-growing textarea (caps ~5 lines then scrolls internally; Enter sends, Shift+Enter newlines; mic/Send seated at the bottom edge); verified in the simulator with a 140-char message fully visible. (4) The owner's screenshot still shows a gap above the keyboard on-device — the dock-pad collapse (T-067) is deployed and verified flush in the simulator, so the standing hypothesis is a stale CSS bundle in their open Safari tab; retest after a fresh tab load, escalate only if it reproduces. | Real-device usage is finding what neither desktop review nor the simulator could (audible output, mic ergonomics) — and the product is simpler and more honest without a voice-output toggle that iOS silently breaks. | Commit 4c280b4; npm run verify green — 25 files, 286/286 (count down by the deleted suite); simulator screenshots in session records. Complete pending prod deploy + smoke this cycle. |
| T-069 | 2026-07-11 12:20-12:30 | User / Fable 5 (mobile workstream) | Owner (on-device): the scroll bounce-back is 'really choppy, not smooth at all.' Root cause was self-inflicted: T-063's document-scroll pin ran scrollTo(0,0) on EVERY window scroll event, so iOS's elastic rubber-band animation was interrupted mid-flight repeatedly — the exact choppiness described. Fix narrows the pin to its actual purpose (countering Safari's focused-input reveal scroll): it now fires only while a text control is focused AND scrollY > 0 (never during the elastic bounce, whose scrollY is <= 0), and the document frame gets overscroll-behavior:none so the page never rubber-bands at all — the transcript (overscroll contained) is the sole scroll surface, per the standard app-like chat frame. Simulator sanity pass: transcript scrolling, prompt list, dock, and keyboard-flush behavior all intact; the smoothness itself is finger-feel the owner must confirm on-device. | The pin fixed the keyboard dead-zone but overreached into gesture territory; scoping it to the focused-input case keeps both behaviors correct instead of trading one defect for another. | Commit d2d671c; npm run verify green — 25 files, 286/286. Complete pending prod deploy + smoke this cycle and owner's on-device feel test. |
| T-070 | 2026-07-11 12:10-12:30 | User / Fable 5 | Owner caught a live trust bug during a demo: the bot answered "Can I have someone follow up with me" by pointing at "a follow-up request form just below this chat" — but NO form rendered. Root cause traced in selectActionCards: the escalation card fired only when a user-intent keyword matched (book/pricing/portal/maturity) OR the assistant disclaimed knowledge (ESCALATION_SIGNAL). That turn matched neither — "follow up" is not an intent keyword and the model answered confidently — so the card selector returned [] while the model's text (ungrounded — no KB entry describes the form) promised a form that was not on screen. Fix (src/lib/actions/select.ts): two explicit escalation triggers added — ESCALATION_REQUEST on USER text (follow up / contact me / reach me / have someone / be in touch / hear back / connect me) and FORM_MENTION on ASSISTANT text (follow-up request form / form below / fill in your / consent box / submit the form). Both fire even alongside an informational card; the original fuzzy ESCALATION_SIGNAL fallback stays gated on "no other card." FORM_MENTION is the invariant the owner asked for: if the bot mentions the form, the form renders every time. 14 new unit tests incl. the exact screenshot turn, an it.each form-mention invariant sweep, and a guard that the plain contact route ("reach out to a strategist at …") stays cardless. Existing regressions preserved (healthy contact-route answer → [], info card preferred over fuzzy escalation). | The bot must never reference UI that is not there — a form it points at is a promise the deterministic selector now keeps. | src/lib/actions/select.ts; src/lib/actions/__tests__/select.test.ts (36 pass); `npm run verify` 25 files / 299 tests exit 0; commit follows; deploy pending. Complete. |
| T-071 | 2026-07-11 12:45-13:10 | User / Fable 5 | Pre-submission presentation pass. Owner decided (after weighing repo-sharing mechanics) to KEEP the honest 85-commit history and polish the finished state rather than squash into a fresh repo — the AI-orchestration trail is a strength for an FDE role and a flattened 3-day history reads as hiding process. Added a curated, leak-safe `skills/` folder documenting the four methods that actually built this repo (codex-review, systematic-debugging, brainstorming, adversarial-verification), each authored fresh to skill standards — frontmatter, when-to-use, the method, how-it-was-used-here with evidence links, and an anti-patterns section (the mistakes each method prevents, several hit and recovered in the open). Authored clean rather than copying personal ~/.claude/skills (which carry business paths + scratch files — leak scan confirmed the new folder is clean: no EI/Cortex/client/secret/personal-path refs). Wired skills/ into README (Layout + the "How it was built" section now frames the orchestration as deliberate methodology and points at skills/). Consistency sweep after the recent UI work: fixed one stale reference — the manual-pass checklist still listed a "speaker toggle" that T-068 removed. Verified AGENTS.md tight (111 lines, CLAUDE.md = @AGENTS.md import), plan.md reflects the read-aloud removal, only open plan item is the Gem submission. | Own the process as the product for an FDE role: the workflow is now a first-class, auditable artifact with anti-patterns, not just an implied one — and the entry points read clean for a reviewer landing cold. | skills/ (README + 4 SKILL.md, leak-scanned clean); README.md; docs/verification/2026-07-10-requirements-verification.md; `npm run verify` 25 files / 299 tests exit 0; commit follows. Complete. |

## Current state at T-018 (historical planning-phase snapshot)

> Kept verbatim from the planning phase; superseded by later entries.

### Complete

- Source brief reviewed directly
- Public Cadre corpus inventoried and captured
- Curated support knowledge and boundaries documented
- Agent components, routing, data flow, and scope decisions documented
- Rubric checklist created
- Fable implementation handoff prepared

### Not started

- Fresh application repository
- Next.js application scaffold
- Model/API integration
- Database or escalation persistence
- Authentication
- Voice implementation
- Tests for the future application
- Vercel deployment
- GitHub submission repository

The phrase “started scaffolding” refers to the planning/research package, not a
claim that the chatbot application has been scaffolded.

## Next-entry rule

The next material action receives ID `T-072`. Before any agent says a task is
complete, it must append an entry containing:

1. What changed
2. Why it changed
3. Files, commands, URLs, or commits that prove it
4. Whether the result is complete, partial, failed, reverted, or blocked
